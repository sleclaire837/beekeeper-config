clusters:
  testnet:
    _inherit: ""
    name: bee
    namespace: private-testnet
    disable-namespace: true
    api-domain: private-testnet.testnet.internal
    api-insecure-tls: true
    api-scheme: https
    debug-api-domain: private-testnet.testnet.internal
    debug-api-insecure-tls: true
    debug-api-scheme: https
    funding:
      eth: 0.1
      gbzz: 2.0
    node-groups:
      gateway:
        mode: node
        bee-config: testnet
        config: testnet
        count: 4
      full-bee:
        mode: node
        bee-config: testnet
        config: testnet
        count: 30

# node-groups defines node groups that can be registered in the cluster
# node-groups may inherit it's configuration from already defined node-group and override specific fields from it
node-groups:
  testnet:
    _inherit: "default"
    persistence-enabled: true

# bee-configs defines Bee configuration that can be assigned to node-groups
# bee-configs may inherit it's configuration from already defined bee-config and override specific fields from it
bee-configs:
  testnet:
    _inherit: "default"
    bootnodes: "/dnsaddr/testnet.ethswarm.org"

checks:
  load:
    options:
      content-size: 50000000
      postage-amount: 10000
      gas-price: 150000000000
      postage-depth: 24
      nodes-sync-wait: 30s
      duration: 12h
      uploader-count: 1
      downloader-count: 0
      upload-groups:
        - gateway
      download-groups:
        - full-bee
    timeout: 12h
    type: load